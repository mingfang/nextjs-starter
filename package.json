{
  "name": "custom-server-express",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon --verbose -L --watch server server/server.js",
    "build": "next build",
    "start": "cross-env NODE_ENV=production node server/server.js"
  },
  "dependencies": {
    "cross-env": "^7.0.2",
    "express": "^4.17.1",
    "express-enrouten": "^1.3.0",
    "next": "latest",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.10"
  }
}
